# 추천 품질 사람 검토 UI 검증

## 범위

리팩터링 후 첫 후속 단계로 현재 자동추천과 사용자의 실제 선택을 비교하는 장면별 검토 기능을 추가했다. 검토는 분류 결과와 사진 원본을 변경하지 않으며 개인 큐에만 저장한다.

## 구현 결과

| 항목 | 결과 |
| --- | --- |
| 진입점 | 결과 갤러리 하단 `추천 품질 검토` 버튼 |
| 검토 단위 | 복수 사진이 있는 장면 군집 |
| 사람 선택 | 장면별 1순위 필수, 2순위 선택 |
| 상세 확인 | 기존 전체 화면 사진 뷰어 연결 |
| 추가 라벨 | 장면 경계와 눈 감김·흐림·표정·중복·기타 오류 |
| 저장 | 선택마다 원자적 JSON 교체, 앱 재시작 후 이어서 검토 |
| 개인정보 | `~/.photos-mcp` 개인 경로, 디렉터리 `0700`, 파일 `0600` |
| 집계 | 자동 1순위 적중률, Top-2 포함률, 사람 선택 재현율, 자동추천 정밀도 |

## 실제 1,000장 결과 연결

2026-08-10 리팩터링 회귀 작업의 1,000장 결과를 그대로 사용했다.

| 항목 | 결과 |
| --- | ---: |
| 전체 결과 | 1,000장 |
| 비교 가능한 복수 사진 장면 | 140개 |
| 검토 대상 사진 | 542장 |
| 미리보기 누락 | 0장 |
| 초기 완료·건너뜀 | 0개 |

Computer Use로 설치 앱의 1,000장 결과를 열어 버튼 노출, 140장면 표시, 자동추천 1·2순위 배지, 두 열 큰 이미지, 사람 1·2순위 선택, 저장 후 다음 장면 이동과 실시간 집계 갱신을 확인했다. 검증용 선택은 종료 전에 백업으로 복원해 실제 사용자 ground truth에는 포함하지 않았다.

## 자동 검증

```bash
PYTHONPATH=src .venv/bin/pytest -q
PYTHONPATH=src .venv/bin/python scripts/validate_docs.py
./scripts/build_framework_standalone.sh
```

| 검사 | 결과 |
| --- | --- |
| 추천 검토·AppKit·레이아웃 집중 검사 | `65 passed in 1.98s` |
| 전체 자동 회귀 | `474 passed in 4.46s` |
| 문서 검증 | 통과 |
| standalone 코드 서명 | 통과 |
| bundle health | 통과 |
| `osxphotos` runtime smoke | 통과 |
| `photo-source`·`photo-ranker-vision` vendor smoke | 통과 |
| production 앱 설치와 재시작 | 통과 |

## 다음 판정

기능 구현은 완료했지만 추천 품질 교정은 아직 시작하지 않는다. 사용자가 최소 50개 장면을 직접 검토한 뒤 생성되는 집계가 현재 기준선이 된다. 이후 동일 라벨을 고정한 상태에서 얼굴 품질, 선명도, 표정과 중복 억제 신호를 shadow score로 비교한다.
